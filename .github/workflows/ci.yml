# Unified CI Workflow — builds, tests, and validates literouter across Linux,
# macOS, and Windows with pinned toolchains, unit tests, and smoke tests.
name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  build-test:
    name: build + test (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-x86_64
          - os: macos-latest
            platform: macos-arm64
          - os: windows-latest
            platform: windows-x86_64

    env:
      # Linux runner has no GPU: force Mesa's software rasterizer so GUI's
      # GLFW/OpenGL startup does not depend on a hardware GLX context.
      LIBGL_ALWAYS_SOFTWARE: '1'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Install mcpp: Unix installer for Linux/macOS, xlings for Windows.
      - name: Install mcpp (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          curl -fsSL https://github.com/mcpp-community/mcpp/releases/latest/download/install.sh \
            | MCPP_NO_PATH=1 bash
          echo "$HOME/.mcpp/bin" >> "$GITHUB_PATH"

      - name: Install xlings and mcpp (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          irm https://d2learn.org/xlings-install.ps1.txt | iex
          $xlingsBin = "$env:USERPROFILE\.xlings\subos\default\bin"
          $env:PATH = "$xlingsBin;$env:PATH"
          $xlingsBin | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          xlings --version
          xlings install mcpp -y
          mcpp --version

      - name: Cache mcpp toolchains and dependency cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.mcpp/registry
            ~/.mcpp/build-cache
          key: mcpp-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('mcpp.toml', '**/mcpp.toml', '**/mcpp.lock') }}
          restore-keys: |
            mcpp-${{ runner.os }}-${{ runner.arch }}-

      - name: Toolchain report
        run: |
          mcpp --version
          mcpp self env

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: 'web/package-lock.json'

      - name: Build web console
        run: |
          npm ci --prefix web
          npm run build --prefix web

      - name: Verify web dist is in sync (Linux)
        if: runner.os == 'Linux'
        run: git diff --exit-code -- web/dist

      - name: Build core
        run: mcpp build -p core

      - name: Build cli
        run: mcpp build -p cli

      - name: Build gui
        # On macOS, zlib shared linkage currently lacks C++ runtime symbols; keep going if it fails
        continue-on-error: ${{ runner.os == 'macOS' }}
        run: mcpp build -p gui

      - name: Test core
        run: mcpp test -p core

      - name: CLI smoke test
        run: |
          mcpp run -p cli -- --version
          mcpp run -p cli -- config init --force
          mcpp run -p cli -- config validate

      - name: GUI smoke test (Linux headless)
        if: runner.os == 'Linux'
        run: |
          command -v xvfb-run >/dev/null || {
            sudo apt-get update -qq
            sudo apt-get install -y -qq xvfb
          }
          xvfb-run -a -s "-screen 0 1600x900x24" \
            env LITEROUTER_GUI_SMOKE=1 mcpp run -p gui

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: literouter-${{ matrix.platform }}
          if-no-files-found: warn
          path: |
            cli/target/**/bin/literouter*
            gui/target/**/bin/literouter-gui*
            gui/target/**/bin/*.dll
